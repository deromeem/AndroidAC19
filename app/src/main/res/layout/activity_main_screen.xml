<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainScreenActivity">

    <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:orientation="vertical"
        android:gravity="center_horizontal">

        <!-- Nom de l'utilisateur connecté -->
        <TextView android:id="@+id/txtUserName"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:layout_margin="5dip"
            android:layout_marginBottom="10dip"
            android:textSize="17dip"/>

        <!--  Bouton pour afficher les messages reçus -->
        <Button android:id="@+id/btnViewMesMessagesRecus"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:text="Mes messages reçus"
            android:layout_marginTop="25dip"/>

        <!--  Bouton pour afficher les messages émis -->
        <Button android:id="@+id/btnViewMesMessagesEmis"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:text="Mes messages émis"
            android:layout_marginTop="25dip"/>

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>